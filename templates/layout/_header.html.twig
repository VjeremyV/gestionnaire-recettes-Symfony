<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
	<div class="container-fluid">
		<div class="collapse navbar-collapse" id="navbarColor02">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_home')}}">Accueil
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_recipe_public')}}">Recettes
					</a>
				</li>

				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link active" href="{{path('app_ingredients')}}">Mes Ingredients
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link active" href="{{path('app_recipe')}}">Mes Recettes
						</a>
					</li>
				{% endif %}
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_contact')}}">Contact
					</a>
				</li>
			</ul>
		</div>
		<div class="d-flex">
			<ul class="navbar-nav me-auto">
				{% if app.user %}
					<li class="nav-item dropdown " style="margin-right : 8rem">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullName}}</a>
						<div class="dropdown-menu">
							<a class="dropdown-item" href="{{path('app_user_edit', {id : app.user.id})}}">Modifier mes informations</a>
							<a class="dropdown-item" href="{{path('app_user_edit_pwd', {id : app.user.id})}}">Modifier mon mot de passe</a>
							<div class="dropdown-divider"></div>
							<a class="dropdown-item" href="{{path('app_logout')}}">Deconnexion</a>
						</div>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link active" href="{{path('app_security')}}">Connexion
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link active" href="{{path('app_registration')}}">Inscription
						</a>
					</li>
				{% endif %}

			</ul>
		</div>
	</div>
</nav>
